/** @type {import('tailwindcss').Config} */
module.exports = {
  theme: {
    extend: {
      cursor: {
        arrow: 'default', // Use 'default' for arrow cursor
      },
    },
  },
  variants: {},
  plugins: [],
};